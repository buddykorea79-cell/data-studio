export default function FileUploadPanel({ file, onFile, onPreview, onIngest, busy }: { file: File | null; onFile: (file: File | null) => void; onPreview: () => void; onIngest: () => void; busy: boolean }) { return <section className="rag-card"><h2>3단계 파일 업로드</h2><label>복지서비스 파일 (CSV, JSON, JSONL, XML, TXT 지원)<input type="file" accept=".csv,.json,.jsonl,.xml,.txt" onChange={(e) => onFile(e.target.files?.[0] || null)} /></label><p>{file ? `선택한 파일: ${file.name}` : "파일을 선택하세요."}</p><button type="button" onClick={onPreview} disabled={!file || busy}>파일 분석</button><h2>4단계 벡터 DB화</h2><button type="button" onClick={onIngest} disabled={!file || busy}>벡터 DB 생성 실행</button><p aria-live="polite">{busy ? "⏳ 진행 중입니다. 파일 크기에 따라 시간이 걸릴 수 있습니다." : "대기 중"}</p></section>; }
