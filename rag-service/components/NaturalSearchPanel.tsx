export default function NaturalSearchPanel({ query, onQuery, onSearch }: { query: string; onQuery: (value: string) => void; onSearch: () => void }) { return <section className="rag-card"><h2>5단계 자연어 검색</h2><label>질문<textarea maxLength={2000} value={query} onChange={(e) => onQuery(e.target.value)} placeholder="예: 서울에 사는 청년이 받을 수 있는 주거 지원은?" /></label><p>{query.length}/2000자</p><button type="button" onClick={onSearch}>벡터 DB 검색</button></section>; }
